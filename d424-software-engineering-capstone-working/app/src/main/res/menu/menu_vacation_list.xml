<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:title="Home"
        android:id="@+id/home_menuItem"
        />
    <item android:title="Add Sample Vacation"
    android:id="@+id/sample_vacation_menuItem"
    />

    <item android:title="Add Vacation"
        android:id="@+id/add_vacation_menuItem"
        />

    <item android:title="Generate Trip Report"
        android:id="@+id/generate_trip_report"
        />
    <item
        android:id="@+id/action_search"
        android:title="Search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="always" />
</menu>